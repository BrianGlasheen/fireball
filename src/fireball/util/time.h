#include <chrono>

using std::chrono::system_clock, std::chrono::time_point,std::chrono::milliseconds, std::chrono::nanoseconds, std::chrono::duration_cast, std::chrono::high_resolution_clock, std::chrono::duration;

typedef time_point<system_clock> Time;

